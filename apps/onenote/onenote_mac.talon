os: mac
and app.bundle: com.microsoft.onenote.mac
-
note new:
    key(cmd-n)

note title:
    key(cmd-shift-t)

note move:
    key(cmd-shift-m)

note copy:
    key(cmd-shift-c)

indent:
    key(cmd-])

dedent:
    key(cmd-[)

search:
    key(cmd-f)

search all:
    key(cmd-alt-f)

bullet:
    key(cmd-.)

book next:
    key(cmd-shift-{)

book last:
    key(cmd-shift-})

notes:
    key(cmd-ctrl-g)

books:
    key(ctrl-shift-g)

next:
    key(down)

last:
    key(up)
